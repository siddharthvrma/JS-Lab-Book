var msg;
msg="<p><code>The actual script is in external script file called common.js</code></p>";

function addNos(headVar,bodyVar)
{
    document.write(this.msg)
    document.write("The sum of the variables <i>headVar</i> and <i>bodyVar</i> is "+(headVar+bodyVar))

}